import React from "react";
import "./Header.css";

const Header: React.FC = () => {
  return (
    <header style={{ marginBottom: "20px", textAlign: "center" }}>
      <h1 style={{ color: "#fff" }}>On-Play</h1>
    </header>
  );
};

export default Header;
